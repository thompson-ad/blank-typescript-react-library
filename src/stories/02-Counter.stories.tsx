import React from "react";
import Counter from "../components/Counter";
import "../index.css";

export default {
  title: "Counter",
  component: Counter
};

export const CounterUsage = () => {
  return <Counter />;
};
